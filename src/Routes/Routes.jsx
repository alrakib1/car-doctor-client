import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
    {
      path: "/",
      element: <div className="text-4xl font-bold flex justify-center">Hello world!</div>,
    },
  ]);
  export default router;